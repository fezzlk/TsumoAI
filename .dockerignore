.git
.gitignore
.env
.pytest_cache
__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
tests
# Personal eval photos, not for the image (Docker can't re-include a file
# under an excluded parent dir, so this must stay narrow — not `data`).
data/eval_images
data/eval_images_cropped
